# Sprint 3 Guardrail Design

## Overview
As part of Sprint 3.3 (FR-18), we designed and implemented a dual-layer guardrail system to secure the Agentic Incident Resolution Platform against adversarial inputs and to prevent data leakage or destructive actions. The system is split into **Input Screening** and **Output Validation**.

## 1. Input Screening (`src/agent/guardrails/input_screening.py`)
This layer runs immediately after an incident is loaded from ServiceNow, before any LLM processing occurs. It serves two primary purposes:

### Injection Screening
We use regular expressions to detect four main categories of Prompt Injections:
- **Instruction Overrides**: (e.g., "ignore previous instructions")
- **Roleplay Hijacks**: (e.g., "act as an admin")
- **Delimiter Breaks**: (e.g., `<|im_start|>`)
- **Prompt Leaks**: (e.g., "reveal your system prompt")

Detected malicious content is neutralized by wrapping it in `[SCREENED_CONTENT]` tags. This ensures the incident is still processed without falling victim to the attack.

### PII & Credential Redaction
We detect and redact sensitive information using regex patterns for:
- API Keys and Access Tokens
- Passwords
- Credit Card Numbers
- Social Security Numbers
- Email Addresses and Phone Numbers

Sensitive strings are replaced with standard placeholder tokens (e.g., `[REDACTED_API_KEY]`).

## 2. Output Validation (`src/agent/guardrails/output_validation.py`)
Before the agent acts on its findings, the generated output must pass a strict validation layer, which is enforced by the `safety_check_node`.

### Action Allowlist
The agent is restricted to a hardcoded list of safe ServiceNow write operations (e.g., `update_incident`, `resolve_incident`). Destructive actions like `delete_incident` or `drop_table` are explicitly blocked.

### Output Content Screening
The text generated by the agent is scanned to ensure it does not accidentally leak system prompts, internal configurations, or credentials.

### Schema Conformance
We verify that the state `outputs` dictionary contains required keys (e.g., `resolution_text`) before executing the action.

## Safety Check Node Enforecement
If output validation fails at any point, `safety_check_node` intercepts the workflow, drops the agent's confidence score to `0.0`, logs the specific `failure_reason`, and flags the action as `blocked_by_guardrail`. This automatically forces the workflow into manual human review, averting any potential damage.

## Observability
Both guardrail layers integrate with **Langfuse**, emitting specialized `guardrail` spans containing the verdict, execution latency, and specific blocked reasons. This ensures complete observability for security audits.
